{
  "name": "my-home-ci-firestore-tests",
  "description": "Tests des regles de securite Firestore — executes contre l'emulateur",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project my-home-ci-test \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.0.0"
  }
}
